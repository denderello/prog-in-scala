object ForPlayaround extends App {
  
  println("Simple for exmaple with .. to .. Ranges:")
  
  for(i <- 1 to 4)
    println(i)
  
  println("Simple for example with .. until .. Ranges:")
  
  for(i <- 1 until 15)
    println(i)
}